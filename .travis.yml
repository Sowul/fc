language: python
python:
  - 2.7
  - 3.5
  - 3.6
  - 3.7
install:
  - pip install -r requirements.txt
  - pip install pytest-cov coveralls
script:
  - python -m pytest --cov=fc --cov=ga
after_success:
  - if [[ "$TRAVIS_PYTHON_VERSION" == "3.7" ]]; then
      coveralls;
    fi